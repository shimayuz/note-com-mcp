# ============================================
# note MCP Server 環境変数設定
# ============================================
# このファイルを .env にコピーして使用してください
# cp .env.sample .env
# ============================================

# --------------------------------------------
# 認証情報（いずれかの方法を選択）
# --------------------------------------------

# 方法A: メールアドレスとパスワード（自動ログイン）
# NOTE_EMAIL=your-email@example.com
# NOTE_PASSWORD=your-password

# 方法B: セッション情報（手動設定）
# ブラウザのDevToolsから取得した値を設定
# NOTE_SESSION_V5=your-session-cookie
# NOTE_XSRF_TOKEN=your-xsrf-token

# --------------------------------------------
# ユーザー設定（オプション）
# --------------------------------------------

# note.com のユーザーID（オプション、自動取得される）
# NOTE_USER_ID=your-note-user-id

# --------------------------------------------
# サーバー設定（オプション）
# --------------------------------------------

# HTTPサーバーのポート番号（デフォルト: 3000）
# MCP_HTTP_PORT=3000

# --------------------------------------------
# 使い方
# --------------------------------------------
# 1. このファイルを .env にコピー
#    cp .env.sample .env
#
# 2. .env ファイルを編集して認証情報を設定
#
# 3. サーバーを起動
#    npm run start
#
# 4. または .env ファイルなしで起動（ブラウザログイン）
#    rm .env && npm run start
# --------------------------------------------
